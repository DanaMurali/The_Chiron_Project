type Props = {
  data?: Array<Record<string, unknown>>;
};

const ProfileList = ({ data }: Props) => {
  return <div>will map profile cards here when have dynamic data</div>;
};

export default ProfileList;
